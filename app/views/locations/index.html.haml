%h1 Listing locations
%button.map Use Current Location!
%input{name: "address", placeholder: "Enter Address", type: "text", id: 'address'}
%input{name: "distance", placeholder: "Distance", type: "integer" id: 'distance'}
%input{name: "length", placeholder: "Length", type: "integer" id: 'length'}
%input{name: "skill", placeholder: "skill", type: "text" id: 'skill'}
%table
  %tr
    %th Latitude
    %th Longitude
    %th Address
    %th Title
    %th
    %th
    %th

  - @locations.each do |location|
    %tr
      %td= location.latitude
      %td= location.longitude
      %td= location.address
      %td= location.title
      %td= link_to 'Show', location
      %td= link_to 'Edit', edit_location_path(location)
      %td= link_to 'Destroy', location, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Location', new_location_path
#map-canvas{style: "height: 500px; width: 500px; background-color: white"}
/ %div{:style => "width: 800px;"}
/   #map{:style => "width: 800px; height: 400px;"}
/ :javascript
/   handler = Gmaps.build('Google');
/   handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
/     markers = handler.addMarkers(#{raw @hash.to_json});
/     handler.bounds.extendWith(markers);
/     handler.fitMapToBounds();
/   });
